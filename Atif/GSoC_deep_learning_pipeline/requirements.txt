# Dataset Foundation (Phase 1) dependencies
# Distances are in millimeters end-to-end; geometry stack is Open3D + NumPy/SciPy.

open3d==0.19.0
numpy<2
scipy>=1.10
torch>=2.0            # available for downstream tensor interop; not required for Phase 1
pyyaml>=6.0
pytest>=8.0
hypothesis>=6.0
