[pytest]
# Make the package under src/ importable without an install step.
pythonpath = src

# Discover tests in the tests/ package.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Silence noisy third-party warnings during test runs.
filterwarnings =
    ignore::DeprecationWarning
